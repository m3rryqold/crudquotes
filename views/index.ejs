<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Learning Crud</title>
    </head>
    <body>
        <h4>Creation</h4>
        <form action="/quotes" method="POST">
            <input type="text" placeholder="name" name="name">
            <input type="text" placeholder="quote" name="quote">
            <button type="submit">Submit</button>
        </form>

        <h4>Reading Quotes</h4>
        <ul class="quotes">
            <% for(var x=0; x<quotes.length; x++) {%>
                <li class="quote">
                    <span><%= quotes[x].name %></span>:
                    <span><%= quotes[x].quote %></span>
                </li>
            <% } %>
        </ul>

        <div>
            <h4>Replacing Quota's Last Quote With Grey's quote(update)</h4>
            <button id="update"> Update </button>
        </div>

        <div>
            <h4>Deleting Grey's 1st quote (Delete)</h4>
            <button id="delete">Delete</button>    
        </div>
        
        <script src="main.js">
            
        </script>
    </body>
</html>